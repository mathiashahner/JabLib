#include <modInclude.h>

void RomansToDecimal (char * buffer, char * output)
{
int I = 1, V = 5, X = 10, count_1, count_2;

   for( count_1 = 0; count_1 < strlen( buffer ); count_1++ )
   {
      if ( 1 )
      {
      `  output = (char *)strlen( buffer );
      }
   }
}
